<!DOCTYPE html>
<html class="light" lang="en">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>Medical RAG System - Login</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#2D3447",
                        "slate-gray": "#A9B8C2",
                        "champagne": "#E1DBC5",
                        "cream": "#F2F2E2",
                        "background-light": "#F2F2E2",
                    },
                    fontFamily: {
                        "display": ["Inter", "sans-serif"]
                    },
                    borderRadius: {
                        "ultra": "4rem",
                        "DEFAULT": "0.25rem",
                        "lg": "0.5rem",
                        "xl": "0.75rem",
                        "full": "9999px"
                    },
                },
            },
        }
    </script>
    <style type="text/tailwindcss">
        :root {
            --primary-navy: #2D3447;
            --slate-blue: #A9B8C2;
            --champagne-beige: #E1DBC5;
            --cream-bg: #F2F2E2;
        }
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--cream-bg);
        }
        .blob-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 0;
        }
        .blob {
            position: absolute;
            border-radius: 50%;
            filter: blur(100px);
            opacity: 0.4;
        }
        .blob-1 {
            width: 700px;
            height: 700px;
            top: -150px;
            left: -150px;
            background: var(--slate-blue);
        }
        .blob-2 {
            width: 600px;
            height: 600px;
            bottom: -100px;
            right: 15%;
            background: var(--champagne-beige);
        }
        .blob-3 {
            width: 500px;
            height: 500px;
            top: 15%;
            right: -100px;
            background: var(--slate-blue);
            opacity: 0.25;
        }
        .glass-card {
            background: rgba(255, 255, 255, 0.96);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-radius: 4.5rem;
        }
        .input-group:focus-within label {
            color: var(--primary-navy);
            transform: translateY(-2px);
        }
        .morph-input {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .morph-input:focus {
            border-radius: 1.5rem;
            box-shadow: 0 0 0 4px rgba(45, 52, 71, 0.08);
            border-color: var(--primary-navy);
        }
    </style>
</head>

<body class="text-primary min-h-screen relative overflow-hidden font-display">
    <div class="blob-container pointer-events-none">
        <div class="blob blob-1"></div>
        <div class="blob blob-2"></div>
        <div class="blob blob-3"></div>
    </div>

    <div class="relative z-10 flex flex-col min-h-screen">
        <header class="flex items-center justify-between px-8 md:px-16 py-8">
            <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-primary rounded-xl flex items-center justify-center text-cream">
                    <span class="text-2xl">🏥</span>
                </div>
                <h2 class="text-lg font-extrabold tracking-tight text-primary">Medical RAG System</h2>
            </div>
            <div class="flex items-center gap-8">
                <nav class="hidden md:flex gap-8 text-sm font-medium text-primary/60">
                    <a class="hover:text-primary transition-colors" href="#">Dashboard</a>
                    <a class="hover:text-primary transition-colors" href="#">Patients</a>
                </nav>
            </div>
        </header>

        <main class="flex-1 flex flex-col md:flex-row items-center px-8 md:px-16 pb-12">
            <div class="w-full md:w-5/12 lg:w-4/12 flex flex-col gap-8 order-2 md:order-1 mt-12 md:mt-0">
                <div class="glass-card p-12 md:p-14 shadow-2xl shadow-primary/10 border border-medical-blue/30">
                    <div class="mb-10 text-center md:text-left">
                        <h1 class="text-4xl font-extrabold tracking-tight mb-3 text-primary">Healthcare Access</h1>
                        <p class="text-sm text-primary/60 leading-relaxed max-w-[280px] md:max-w-none mx-auto md:mx-0">
                            Enter your credentials to access the patient monitoring dashboard.
                        </p>
                    </div>

                    <form action="/login" method="POST" class="space-y-6">
                        <div class="input-group flex flex-col gap-2">
                            <label
                                class="text-xs font-bold uppercase tracking-widest text-primary/50 transition-all duration-300 ml-1">
                                Username
                            </label>
                            <input
                                class="morph-input w-full bg-white border border-slate-400/40 h-14 px-6 rounded-2xl focus:outline-none text-base text-primary placeholder:text-primary/30"
                                placeholder="admin" type="text" name="username" id="username" required
                                autocomplete="username" />
                        </div>

                        <div class="input-group flex flex-col gap-2">
                            <div class="flex justify-between items-center ml-1 pr-1">
                                <label
                                    class="text-xs font-bold uppercase tracking-widest text-primary/50 transition-all duration-300">
                                    Password
                                </label>
                            </div>
                            <div class="relative">
                                <input
                                    class="morph-input w-full bg-white border border-slate-400/40 h-14 px-6 pr-12 rounded-2xl focus:outline-none text-base text-primary placeholder:text-primary/30"
                                    placeholder="••••••••" type="password" name="password" id="password" required
                                    autocomplete="current-password" />
                                <button
                                    class="absolute right-5 top-1/2 -translate-y-1/2 text-primary/40 hover:text-primary transition-colors"
                                    type="button" onclick="togglePassword()">
                                    <span class="material-symbols-outlined text-xl"
                                        id="visibilityIcon">visibility</span>
                                </button>
                            </div>
                        </div>

                        <div class="flex items-center gap-3 py-2 ml-1">
                            <input class="w-4 h-4 rounded-md border-slate-400/40 text-primary focus:ring-primary"
                                id="remember" type="checkbox" />
                            <label class="text-xs font-medium text-primary/60 cursor-pointer" for="remember">
                                Stay signed in for 30 days
                            </label>
                        </div>

                        <button
                            class="w-full h-14 bg-primary hover:bg-opacity-90 text-cream font-bold rounded-2xl shadow-lg shadow-primary/20 transition-all active:scale-[0.98] flex items-center justify-center gap-2">
                            Sign In to Dashboard
                            <span class="material-symbols-outlined">arrow_forward</span>
                        </button>
                    </form>
                </div>
            </div>

            <div
                class="hidden md:flex flex-1 flex-col items-center justify-center relative order-1 md:order-2 pl-12 lg:pl-24">
                <div class="max-w-lg space-y-8">
                    <div class="relative">
                        <div
                            class="absolute -top-12 -left-12 w-24 h-24 border-t-2 border-l-2 border-slate-gray/30 rounded-tl-3xl">
                        </div>
                        <h2 class="text-5xl lg:text-7xl font-extrabold tracking-tight leading-[1.1] text-primary">
                            Monitoring <span class="italic text-slate-gray">patient</span> health in real-time.
                        </h2>
                        <div
                            class="absolute -bottom-8 -right-8 w-16 h-16 border-b-2 border-r-2 border-slate-gray/30 rounded-br-2xl">
                        </div>
                    </div>

                    <div class="flex gap-12 pt-8">
                        <div>
                            <div class="text-3xl font-bold text-primary">24/7</div>
                            <div class="text-xs font-bold uppercase tracking-widest text-primary/40">Monitoring</div>
                        </div>
                        <div>
                            <div class="text-3xl font-bold text-primary">AI</div>
                            <div class="text-xs font-bold uppercase tracking-widest text-primary/40">Predictions</div>
                        </div>
                    </div>

                    <div class="pt-12">
                        <div class="flex items-center gap-3">
                            <div class="w-12 h-12 rounded-full bg-green-500/20 flex items-center justify-center">
                                <span class="text-2xl">🟢</span>
                            </div>
                            <div class="w-12 h-12 rounded-full bg-yellow-500/20 flex items-center justify-center">
                                <span class="text-2xl">🟡</span>
                            </div>
                            <div class="w-12 h-12 rounded-full bg-red-500/20 flex items-center justify-center">
                                <span class="text-2xl">🔴</span>
                            </div>
                        </div>
                        <p class="text-xs mt-3 text-primary/50 italic">Real-time severity classification system.</p>
                    </div>
                </div>
            </div>
        </main>

        <footer
            class="px-8 md:px-16 py-8 flex flex-col md:flex-row justify-between items-center gap-4 text-[10px] font-bold uppercase tracking-widest text-primary/40">
            <div>© 2024 Medical RAG System. All Rights Reserved.</div>
            <div class="flex gap-8">
                <a class="hover:text-primary transition-colors" href="#">Privacy Policy</a>
                <a class="hover:text-primary transition-colors" href="#">System Status</a>
            </div>
        </footer>
    </div>

    <script>
        function togglePassword() {
            const passwordInput = document.getElementById('password');
            const visibilityIcon = document.getElementById('visibilityIcon');

            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                visibilityIcon.textContent = 'visibility_off';
            } else {
                passwordInput.type = 'password';
                visibilityIcon.textContent = 'visibility';
            }
        }
    </script>
</body>

</html>